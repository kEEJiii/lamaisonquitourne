---
import Base from './Base.astro';
const { frontmatter } = Astro.props;
---
<Base title={frontmatter.title} description={frontmatter.description}>
  <div class="wrap prose" style="padding-top:2.5rem">
    <slot />
  </div>
</Base>
